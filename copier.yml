project_name:
  type: str
  help: "What is your project name?"
  default: "MyProject"

project_type:
  type: str
  help: "What is your project type?"
  choices:
    - "Audio Plugin"
    - "GUI Application"
  default: "Audio Plugin"
